import styled from "styled-components";

const ScrollUpWidget = styled.div`
  .ScrollUpButton__Container {
    background: #777777 !important;
    border-radius: 5px;
    outline: none;
  }
`;

export default ScrollUpWidget;
